# Left command + Opt + hjkl을 화살표 키로 매핑
lcmd - h : skhd -k "left"
lcmd - j : skhd -k "down"  
lcmd - k : skhd -k "up"
lcmd - l : skhd -k "right"
# lcmd + lalt - h : skhd -k "left"
# lcmd + lalt - j : skhd -k "down"  
# lcmd + lalt - k : skhd -k "up"
# lcmd + lalt - l : skhd -k "right"

# App shortcuts
rshift - a: open -a 'Arc'
rshift - b: open -a 'Things3'
rshift - c: open -a 'Calendar'
rshift - d: open -a 'DataGrip'
rshift - f: open -a 'Finder'
rshift - g: open -a 'Google Chrome'
rshift - i: open -a 'IntelliJ IDEA Ultimate'
rshift - m : open -a 'Mail'
rshift - o : open -a 'Obsidian'
rshift - s : open -a 'Slack'
rshift - t : open -a 'Microsoft Teams'
rshift - v : open -a 'Cursor'
rshift - w : open -a 'WezTerm'
rshift - x : open -a 'Xmind'
rshift - y : open -a 'KakaoTalk'
rshift - z : open -a 'Flow'
#
# skhd --restart-service 
#
# focus window
# shift + alt - h : yabai -m window --focus west
# shift + alt - j : yabai -m window --focus south
# shift + alt - k : yabai -m window --focus north
# shift + alt - l : yabai -m window --focus east

# toggle window zoom
# shift + alt - g : yabai -m window --toggle windowed-fullscreen
# shift + alt - f : yabai -m window --toggle zoom-fullscreen

# toggle window split type
# shift + alt - e : yabai -m window --toggle split
