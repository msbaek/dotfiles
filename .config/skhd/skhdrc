# Left command + Opt + hjkl을 화살표 키로 매핑
lcmd + lalt - h : skhd -k "left"
lcmd + lalt - j : skhd -k "down"  
lcmd + lalt - k : skhd -k "up"
lcmd + lalt - l : skhd -k "right"

# App shortcuts
lalt + lcmd + lctrl + lshift - a: open -a 'Arc'
lalt + lcmd + lctrl + lshift - b: open -a 'Things3'
lalt + lcmd + lctrl + lshift - c: open -a 'Calendar'
lalt + lcmd + lctrl + lshift - d: open -a 'DataGrip'
lalt + lcmd + lctrl + lshift - e: open -a 'Finder'
lalt + lcmd + lctrl + lshift - f: open -a 'Flow'
lalt + lcmd + lctrl + lshift - g: open -a 'Google Chrome'
lalt + lcmd + lctrl + lshift - i: open -a 'IntelliJ IDEA Ultimate'
lalt + lcmd + lctrl + lshift - m: open -a 'Mail'
lalt + lcmd + lctrl + lshift - o: open -a 'Obsidian'
lalt + lcmd + lctrl + lshift - p: open -a 'Preview'
lalt + lcmd + lctrl + lshift - s: open -a 'Slack'
lalt + lcmd + lctrl + lshift - t: open -a 'Microsoft Teams'
lalt + lcmd + lctrl + lshift - v: open -a 'Cursor'
# lalt + lcmd + lctrl + lshift - w: open -a 'WezTerm'
lalt + lcmd + lctrl + lshift - w: open -a 'Ghostty'
lalt + lcmd + lctrl + lshift - x: open -a 'Xmind'
lalt + lcmd + lctrl + lshift - y: open -a 'KakaoTalk'
lalt + lcmd + lctrl + lshift - z: open -a 'Flow'
#
# skhd --restart-service 
#
# focus window
# shift + alt - h : yabai -m window --focus west
# shift + alt - j : yabai -m window --focus south
# shift + alt - k : yabai -m window --focus north
# shift + alt - l : yabai -m window --focus east

# toggle window zoom
# shift + alt - g : yabai -m window --toggle windowed-fullscreen
# shift + alt - f : yabai -m window --toggle zoom-fullscreen

# toggle window split type
# shift + alt - e : yabai -m window --toggle split
