# 2026-05 Session Journal

## 2026-05-01 09:07 | dotfiles | sub-agent 위임 패턴 일괄 적용

bottom-up 방식으로 Opus 4.7 세션에서 진행. Trigger는 obsidian commands 2개에 sub-agent 패턴 적용한 것을 전체 user-authored commands/skills로 확장.

브레인스토밍 → writing-plans → subagent-driven-development 워크플로우로 9개 task, 14 commits, 38 파일 수정 + templates/delegation.md 신규.

주요 발견: 버전 suffix 제거(`sonnet-4.6`→`sonnet`), `[options]` placeholder expand, session-handoff 인터랙티브 오분류 수정(자동 정리 작업이라 변형 A가 맞음).

Task 10(행동 검증)은 일상 사용 중 ongoing. N 그룹 reference doc skills 정리는 다음 세션 follow-up.

## 2026-05-02 09:00 | msbaek_vault | 2026-W18 뉴스레터 생성

서브 에이전트 3개 병렬 실행으로 2026-04-25~05-01 주간 분석. 이번 주 핵심 패턴: Uncle Bob, Ian Cooper, Matt Pocock, Emily Bache, Evan You가 독립적으로 "AI 시대에 소프트웨어 엔지니어링 펀더멘탈(TDD, 설계, 이론 구축)이 더 중요해진다"는 결론에 수렴. 실무: ISMS 암호화 배포 진행, 세션 컨텍스트 토큰 최적화(67K→명시 호출), CLAUDE.md Opus 4.7 마이그레이션. coffee-time 없음.

## 2026-05-07 11:29 | skills-audit | 스킬 통계 및 정리 권장

지난 30일간 스킬 사용 현황 분석 완료:
- 활성 스킬: 1개 (test-skill-e2e)
- 미사용: 146개 (99.3
## 2026-05-07 11:29 | skills-audit | 스킬 통계 및 정리 권장

지난 30일간 스킬 사용 현황 분석 완료:
- 활성 스킬: 1개 (test-skill-e2e)
- 미사용: 146개 (99.3%)
- 완전 중복: 5쌍 (atlassian: vs skills: 네임스페이스)
- 정책 삭제 권장: arscontexta: 26개 (완전 미사용)

즉시 액션: skills:* 5개 + databricks-academy 1개 삭제
1주 후 재평가: arscontexta 26개 삭제 여부 결정

상세 리포트: /tmp/skills-audit-report.md
## 2026-05-09 20:17 | BO-query/settlement | 4월 정산 cross-join 부풀림 분석 + 회계팀 위임 결정

에이블리 컴플레인(25616 4건/2건 추출)에서 시작. 운영자 B2B 벌크 입력 시 SELL_DADDR_ID 중복 → settlement-08의 ID 기반 JOIN 으로 cross-join 부풀림. 영향 ID 3건 검증 완료(25616, 20260211001970000215, 20260410001970004316).

옵션 검토: A(Excel 수작업) → B(전체 파이프라인 NO 컬럼 추가) → C(settlement-08만 amount filter 패치). 사용자가 "1-2회/년 빈도"라며 over-engineering 지적 → 회계팀 보강시트 송부 + 피드백 기반 결정으로 단순화.

산출물: settlement-09.sql (영향 ID 탐지 + NO별 펼친 형태 쿼리), settlement-2026-04-101-cross-join-fix.sql (Excel 수작업 playbook), settlement-2026-04-cross-join-fix-plan.md (분석 문서). main 에 commit & push 완료.

다음: 회계팀 피드백 대기. 5월에 동일 패턴 재발 시 settlement-09.sql 재실행으로 자동 탐지 가능.

## 2026-05-09 | dotfiles | spike-and-stabilize skill 설계 및 구현

brainstorming → writing-plans → subagent-driven-development 전체 흐름으로 spike-and-stabilize custom skill 완성.
두 phase: Phase 1 quick & dirty E2E(acceptance test 안전망) → Transition Gate(사용자 승인) → Phase 2 writing-plans/executing-plans 위임.
CLAUDE.md Walking Skeleton 단계에 spike 참조 추가. 코드 퀄리티 리뷰에서 keep 경로 불일치 발견 → 수정 완료.

## 2026-05-09 22:11 | dotfiles | spike-implementer agent 구현 완료

spike-and-stabilize skill의 Phase 1 전담 sub-agent 구현. brainstorming → spec → writing-plans → 직접 실행 전체 흐름 완료.
- ~/.claude/agents/spike-implementer.md: model:sonnet, 고정 출력 형식(Phase 1 결과), STATUS 3종(DONE/DONE_WITH_CONCERNS/BLOCKED)
- SKILL.md 절차 5 후 dispatch 안내 한 줄 삽입 (구현 단계는 spike-implementer agent로 위임)
- plan 파일: docs/superpowers/plans/2026-05-09-spike-implementer-agent.md
- commit: 102dcdc
## 2026-05-10 | dotfiles + cc-orchestra | cc-orchestra skill 구현 완료

writing-plans → subagent-driven-development 전체 흐름으로 cc-orchestra Claude skill 완성.
11개 태스크: lib.sh(공통 함수) → up.sh(session/window 모드) → send/list/down/add/remove.sh → functions.zsh → .zshrc 통합.
Spec 리뷰에서 ccup --session flag 시 CC_ORCHESTRA_SCOPE 미설정 버그 발견 → 즉시 수정.
이제 ccup isms184 enc-mask ~/git/kt4u/enc-mask pacman ~/git/kt4u/pacman 한 줄로 멀티-프로젝트 CC 환경 구성 가능.

## 2026-05-12 17:47 | dotfiles | claude agents 버그 — v2.1.139 native → brew v2.1.126 회귀

`claude agents` (FleetView, EXPERIMENTAL_AGENT_TEAMS=1) 실행 시 worker가 옛 npm 경로 `/opt/homebrew/lib/node_modules/@anthropic-ai/claude-code/bin/claude.exe`를 posix_spawn 시도 → ENOENT 무한 respawn.
- 진단: native binary strings에 `claude.exe` 5번 박혀 있고 `.local`/`win32`/`windows` 컨텍스트와 묶임. native install + macOS 조합에서 platform 분기가 옛 npm 경로 가정으로 깨짐.
- 해결: brew --cask claude-code (stable v2.1.126) 설치 → ~/.local/bin/claude symlink + ~/.local/share/claude/versions/{2.1.116, 2.1.139} 제거 → ~/.claude.json installMethod: native → brew. 데이터(~/.claude/*)는 보존, ~/.claude.json 백업 생성.
- 확인 필요: 새 터미널에서 `claude agents` 정상 동작 검증.


## 2026-05-12 17:52 | dotfiles | claude agents 버그 — 결론: flag 끄고 native 복귀

brew v2.1.126 binary에도 동일한 `claude.exe` 13곳 박혀 있음 → 다운그레이드는 무의미. v2.1.126 이전부터 있던 버그. 사용자 결정: claude agents (FleetView) 기능 자체를 보류, native 최신으로 복귀.
- ~/.claude/settings.json에서 `CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS=1` 제거 (백업 .bak-20260512-175157)
- brew --cask uninstall claude-code
- 사용자가 새 터미널에서 native installer 재실행 예정
- 이후 새 native install + flag 부재 → worker crash 발생 안 함 (FleetView 자체 비활성화)
## 2026-05-14 14:48 | vault | Databricks AI Features 전체 지도 작성 (8-agent 병렬 리서치)

의뢰: Databricks AI 8개 영역 20개 기능/하위메뉴를 Obsidian "전체 기능 지도" 한 문서로. → `001-INBOX/Databricks-AI-Features-전체-지도.md` 생성.
- 8개 영역 = 8개 독립 도메인 → general-purpose(sonnet) agent 8개 단일 메시지 병렬 dispatch, main context(opus)에서 종합. dispatching-parallel-agents skill 적용. 좌측 내비 하위메뉴까지 빠짐없이 조사 지시.
- a-ha: 의뢰 자료의 단정 사실 3건이 현행 공식 문서와 불일치 — Agent Bricks/KA 서울 "미지원"→cross-geo 조건부 지원, Genie Agent Mode "Beta"→Public Preview, AI Gateway 단일 "Beta"→신규 Unity AI Gateway(Beta)+기존 Model Serving용(GA) 병존. advisor 권고로 문서 도입부 정정 콜아웃에 모아 surface.
- 서울 리전 컬럼은 지원/조건부/미지원/불명 4-state로 확장(cross-geo routing 표기가 광범위).
- obsidian-forward-related-injector: 신규 파일이 vis 인덱스 미등록 → reranker 전부 음수 → skill 절차(점수 0 이하 제외) 준수해 Related Notes 보류.
## 2026-05-16 10:40 | dotfiles | check-subagent-model 스킬 리팩토링

Claude에서 사용하던 inline Python heredoc 스킬을 standalone 스크립트(`~/.claude/bin/check-subagent-model.py`)로 분리. 스킬은 thin wrapper로 변경되어 `python3 ~/.claude/bin/check-subagent-model.py $ARGUMENTS` 한 줄만 호출. 사용자가 터미널에서 직접 실행해도 스킬과 동일 결과 → 일관성 확보.
- 출력에 폴더(`cwd`, ~단축), git branch(non-main만), 첫 user prompt(80자) 추가
- ANSI 컬러: sonnet=초록·opus=노랑·haiku=청록. TTY + NO_COLOR env var 자동 감지, `--no-color` 플래그도 지원
- argparse로 `--last N` / `[project-path]` 위치 인자 지원

## 2026-05-16 11:30 | plan-docs | ktown4u 도메인 지식 SSOT 문서 구축 완료

의뢰: Notion + vault work-log + kt4u 레포에서 ktown4u 도메인 지식을 추출해 plan-docs에 저장.
- `docs/domain-knowledge/` 폴더 신설: overview.md(전체 지도), glossary.md(유비쿼터스 언어 사전), INDEX.md.
- Notion 페이지 로그인 후 JavaScript DOM eval로 콘텐츠 추출: SELL_ID 구성 규칙, 6가지 가격 유형, 강판 개념, 마이너스 발주, 적정재고 공식, GMS 진행재고/가용재고 구분.
- 5개 DB 스키마(hmmall·catalog·gms·capybara·thomas) 요약, Bounded Context 맵, 주문 수량 불변식(`QTY = REQUEST_QTY + ZERO_QTY + DELIY_REQUEST_QTY + DELIY_ZERO_QTY`) 모두 포함.
- a-ha: Notion 로그인 필요 → JS eval로 블록 텍스트 직접 추출하는 방식이 playwright screenshot보다 훨씬 효과적.

## 2026-05-17 10:05 | dotfiles | spec-to-feature-review tool 구축 완료

사용자 핵심 동기: spec/plan markdown(verbose)을 행위 중심 Gherkin Feature로 변환하여 리뷰 효율 ↑. 초기에 거대한 BDD skill spec(712줄, 자동 호출 + CLAUDE.md wiring + trial protocol + rollback) 설계했으나 over-engineering 판단으로 deferred. 작은 도구로 pivot.
- 산출물: /spec-to-feature-review slash command + spec-to-feature-review-builder sonnet sub-agent (변형 A delegation)
- 흐름: markdown → use case 추출 → primary + alternative scenarios → Gherkin Feature 변환 → /html-review template 재사용 HTML 리뷰 페이지 → 브라우저 자동 열림
- 결정: Gherkin 영어 키워드(Feature/Scenario/Given/When/Then) + 한국어 본문 — Cucumber 표준 호환 + 한국 개발자 가독성
- Dogfooding 성공: 도구가 자기 자신의 spec을 변환하여 4 scenarios HTML 리뷰 생성. 사용자 만족.
- a-ha 1: agent frontmatter는 single-line description 안에 Examples를 \\n escape으로 통합해야 함. multi-line YAML description + 별도 Examples: field는 parsing 실패 → "Agent type not found". 기존 commit 637600f(7개 agent 정규화)가 단서였음.
- a-ha 2: agent registration은 세션 시작 시 한 번 scan + cache. 새 agent는 새 세션에서만 인식.
- a-ha 3: subagent-driven-development는 단순 작업(50줄 wrapper, 1-line fix)에서는 over-engineered. main context 직접 실행이 효율적. user 선택대로 가되 trade-off 짚어주는 게 정직.
