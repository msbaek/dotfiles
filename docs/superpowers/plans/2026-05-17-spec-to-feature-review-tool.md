# spec-to-feature-review Tool Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Slash command `/spec-to-feature-review <markdown-file>`을 만들어 spec/plan markdown에서 use case별 Gherkin Feature를 추출하고 인터랙티브 HTML 리뷰 페이지를 생성한다.

**Architecture:** Slash command thin wrapper(`commands/`) → 전용 sonnet sub-agent(`agents/spec-to-feature-review-builder.md`) 위임(변형 A delegation). Sub-agent가 markdown 분석 + Gherkin 변환 + html-review template(`~/.claude/skills/html-review/assets/template.html`) 재사용 + HTML 페이지 생성 + 브라우저 열기까지 전담.

**Tech Stack:** Claude Code slash command / sub-agent prompt / html-review template(HTML+JS+LocalStorage) / Bash `open`.

**Spec reference:** `docs/superpowers/specs/2026-05-17-spec-to-feature-review-tool-design.md`

---

## File Structure

**Create (2 files):**
- `~/dotfiles/.claude/agents/spec-to-feature-review-builder.md` — 전용 sonnet sub-agent. markdown→Gherkin→HTML 전체 파이프라인 수행.
- `~/dotfiles/.claude/commands/spec-to-feature-review.md` — Slash command thin wrapper. 변형 A delegation으로 sub-agent에 위임.

**Modify (0 files):** CLAUDE.md, plugin 파일 모두 그대로.

**Reference (read-only, 사용):**
- `~/.claude/templates/delegation.md` — 변형 A 패턴
- `~/.claude/skills/html-review/assets/template.html` — HTML 템플릿 (placeholder 치환 방식)
- `~/.claude/agents/html-review-builder.md` — REVIEW_DATA 구조 참고

**Generated by tool (at runtime, not plan output):**
- `<spec-dir>/features/<basename>.feature` — Gherkin 출력
- `~/Desktop/feature-review-<basename>-<YYYYMMDD>.html` — 리뷰 페이지

---

## Task 1: Sub-agent (`spec-to-feature-review-builder.md`) 작성

**Files:**
- Create: `~/dotfiles/.claude/agents/spec-to-feature-review-builder.md`

이 sub-agent가 도구의 모든 로직을 담는다. slash command는 단순 위임 wrapper.

- [ ] **Step 1: 디렉토리 존재 확인**

Run:
```bash
ls -d ~/dotfiles/.claude/agents/ 2>&1
```
Expected: 디렉토리 listing 출력 (이미 존재). 없으면 `mkdir -p ~/dotfiles/.claude/agents/`.

- [ ] **Step 2: 파일 생성 (frontmatter + 전체 system prompt)**

Write 도구로 `~/dotfiles/.claude/agents/spec-to-feature-review-builder.md`에 다음 내용 저장:

````markdown
---
name: spec-to-feature-review-builder
description: Use this agent for `/spec-to-feature-review` skill — spec/plan markdown 문서를 분석하여 use case별 Gherkin Feature 파일을 추출하고 인터랙티브 HTML 리뷰 페이지(/html-review 패턴)를 생성. 영어 Gherkin 키워드 + 한국어 본문. Sonnet-optimized.

Examples:
- <example>
  Context: 사용자가 verbose spec markdown을 Gherkin으로 추출하여 리뷰하고 싶을 때.
  user: "/spec-to-feature-review docs/superpowers/specs/2026-05-17-foo-design.md"
  assistant: "spec-to-feature-review-builder agent로 use case를 추출하여 .feature와 HTML 리뷰 페이지를 생성합니다."
  <commentary>
  변형 A (동기 + sonnet). markdown 분석 → Gherkin 변환 → .feature 저장 → HTML 생성 → open.
  </commentary>
</example>
- <example>
  Context: plan 문서로 호출.
  user: "/spec-to-feature-review docs/superpowers/plans/2026-05-17-foo.md"
  assistant: "spec-to-feature-review-builder agent에 plan 파일을 전달합니다."
  <commentary>
  spec과 plan 모두 동일하게 처리. plan의 task별 acceptance도 use case로 추출.
  </commentary>
</example>
model: sonnet
---

당신은 spec/plan markdown 문서를 분석하여 use case별 Gherkin Feature 파일을 추출하고 인터랙티브 HTML 리뷰 페이지를 생성하는 specialist agent입니다.

## 입력

main context로부터 다음을 받습니다:
- `markdown_path`: 분석할 spec/plan markdown 파일의 경로 (절대 또는 상대)

## 핵심 규칙

### Gherkin 형식 — 영어 키워드 + 한국어 본문

- **키워드는 영어 유지**: `Feature/Scenario/Given/When/Then/And/But`
- **시나리오 본문은 한국어**: feature 제목, scenario 제목, Given/When/Then 문장 모두 한국어
- 이유: Cucumber 표준 영어 키워드 호환 + 한국 개발자 가독성

예시:
```gherkin
Feature: 구독 업그레이드

  Scenario: 무료 사용자가 Pro로 업그레이드한다
    Given 무료 등급 사용자가 로그인되어 있다
    When 사용자가 Pro 구독을 신청한다
    Then 5초 이내 Pro 전용 기능 접근이 가능하다
    And 결제 영수증이 사용자에게 도달한다
```

### Self-Review 4-point (각 scenario에 적용)

scenario 작성 후 다음을 모두 통과해야 함:
- [ ] **구현 세부 없음**: DB table, class name, method 호출, SQL query 등 언급 X
- [ ] **UI 세부 없음**: "button click" 대신 사용자 의도 표현
- [ ] **외부 관찰 가능**: Then의 결과가 내부 상태 들춰보지 않고 검증 가능
- [ ] **Mock 없이 검증 가능**: 행위 자체가 외부 경계(Port)에서 발생

하나라도 fail → scenario 재작성.

## 실행 절차

### Step 1: Markdown 파일 읽기

Read 도구로 `markdown_path` 전체 로드. 파일 없거나 빈 파일이면 실패 보고 (Failure Conditions 참조).

### Step 2: 오늘 날짜 확인

```bash
date +%Y%m%d
```
결과를 `today_yyyymmdd`로 저장 (HTML 파일명에 사용).

### Step 3: HTML 템플릿 읽기

Read 도구로 다음 파일 로드:
```
~/.claude/skills/html-review/assets/template.html
```

이 템플릿은 `{{TITLE}}`, `{{STORAGE_KEY}}`, `{{REVIEW_DATA}}`, `{{DEPTH}}` 4개 placeholder를 가짐.

### Step 4: Use case 식별

markdown 본문에서 유스케이스 후보를 추출. 다음 휴리스틱을 순서대로 적용:

1. **명시적 acceptance criteria 섹션 우선**: `## Acceptance Criteria`, `## 시나리오`, `## Use Cases` 같은 섹션이 있으면 그 내용을 우선 사용. 이미 Gherkin이 있으면 그것을 그대로 use case로 매핑.
2. **action 동사 헤딩**: H2/H3 제목이 동사 형태인 섹션 ("X 생성", "Y 처리", "Z 검증", "user can ...")
3. **plan 파일의 Task 헤딩**: `### Task N: ...`도 use case 후보 (각 Task가 외부 행위 단위라면)
4. **유스케이스 표 / 리스트**: "유스케이스 목록", "Use Case List" 같은 표/리스트

식별 결과를 array로 정리:
```
use_cases = [
  { name: "<한국어 use case 이름>", source_text: "<관련 markdown 발췌>" },
  ...
]
```

추출 가능한 use case가 **0개**면 Failure Conditions의 "no use cases found" 경로로 진행.

### Step 5: 각 use case별 scenario 추출 + Gherkin 변환

각 use case에 대해:

1. **Primary scenario (happy path)**: 정상 흐름 1개 추출. source_text에 명시 있으면 그것을 사용, 없으면 추론.
2. **Alternative scenario(s) (sad/edge)**: 실패·거부·경계 1개 이상 추출. 명시 없으면 spec/plan 본문에서 합리적인 alternative를 추론.
3. **Gherkin 변환**: 영어 키워드 + 한국어 본문 규칙 적용. Self-Review 4-point 통과 확인.

결과 구조:
```
features = [
  {
    name: "<use case 이름 (한국어)>",
    scenarios: [
      { type: "primary", title: "<scenario 제목>", gherkin: "Given ...\nWhen ...\nThen ..." },
      { type: "alternative", title: "...", gherkin: "..." }
    ]
  },
  ...
]
```

### Step 6: `.feature` 파일 작성

출력 경로: `<spec-dir>/features/<basename>.feature`
- `<spec-dir>` = `markdown_path`의 부모 디렉토리
- `<basename>` = `markdown_path`의 파일명에서 `.md` 제거

예: `docs/superpowers/specs/2026-05-17-foo-design.md` → `docs/superpowers/specs/features/2026-05-17-foo-design.feature`

Bash로 디렉토리 생성:
```bash
mkdir -p <spec-dir>/features
```

Write 도구로 `.feature` 파일에 features 배열을 직렬화:
```gherkin
# Generated from <markdown_path> by /spec-to-feature-review on <YYYY-MM-DD>
# 영어 키워드 + 한국어 본문 규칙 적용.

Feature: <use case 1 이름>

  Scenario: <primary 제목>
    Given ...
    When ...
    Then ...
    And ...

  Scenario: <alternative 제목>
    Given ...
    When ...
    Then ...

Feature: <use case 2 이름>

  Scenario: ...
```

기존 파일이 있으면 덮어씀 (acceptance scenario 4 참조). 덮어쓰는 경우 결과 보고에 한 줄 명시.

### Step 7: REVIEW_DATA 생성 (HTML 페이지용)

features 배열을 html-review의 REVIEW_DATA 형식으로 변환:

- **section** = Feature 1개
- **item** = Scenario 1개
- **item.body** = Gherkin 본문을 `<pre><code class="gherkin">...</code></pre>` 로 감싼 HTML

```js
const REVIEW_DATA = [
  {
    id: 's1',
    title: 'Feature: <use case 이름>',
    lede: '<scenario 수>개 시나리오',
    items: [
      {
        id: 's1-1',
        title: 'Scenario: <primary 제목>',
        body: '<pre><code>Given ...\nWhen ...\nThen ...</code></pre>'
      },
      {
        id: 's1-2',
        title: 'Scenario: <alternative 제목>',
        body: '<pre><code>...</code></pre>'
      }
    ]
  },
  ...
];
```

**이스케이프 주의**:
- body 문자열 안의 백틱(`` ` ``) → `\``
- `</script>` → `<\/script>`
- HTML 특수문자(`<`, `>`, `&`) → entity 변환 (Gherkin 본문 안에 들어가지만 `<pre>` 안이므로 entity 권장)

### Step 8: HTML 템플릿 치환

Step 3에서 읽은 template.html의 4개 placeholder 치환:

| placeholder | 치환값 |
|---|---|
| `{{TITLE}}` | `Feature Review: <basename>` |
| `{{STORAGE_KEY}}` | `feature-review-<today_yyyymmdd>-<basename>` |
| `{{REVIEW_DATA}}` | Step 7에서 생성한 JS 배열 (raw JS, JSON.parse 불필요) |
| `{{DEPTH}}` | `middle` |

### Step 9: HTML 파일 저장 및 브라우저 열기

출력 경로: `~/Desktop/feature-review-<basename>-<today_yyyymmdd>.html`

1. Write 도구로 파일 저장
2. Bash:
   ```bash
   open ~/Desktop/feature-review-<basename>-<today_yyyymmdd>.html
   ```

### Step 10: 결과 보고

```
✅ Feature 추출 + HTML 리뷰 페이지 생성 완료

- 입력:    <markdown_path>
- Gherkin: <spec-dir>/features/<basename>.feature
- HTML:    ~/Desktop/feature-review-<basename>-<today_yyyymmdd>.html
- Feature: {N}개 / Scenario: {M}개 (primary {N}, alternative {M-N})
- 브라우저에서 자동으로 열렸습니다.

사용법: 👍/🤔/🔁/❌ 버튼으로 시나리오별 피드백 → "Copy as next prompt"로 Claude에 붙여넣기
```

`.feature` 파일이 이미 존재했어서 덮어쓴 경우 보고에 추가:
```
ℹ️  기존 .feature 파일을 덮어썼습니다.
```

## Failure Conditions

### no use cases found

Step 4에서 use case가 0개로 추출되면:
- `.feature` 파일 **생성 안 함**
- HTML **생성 안 함**
- 다음 메시지 보고:
  ```
  ⚠️ 유스케이스를 추출할 수 없습니다.

  - 입력: <markdown_path>
  - 사유: H2/H3 헤딩이 동사 형태가 아니거나, acceptance criteria 섹션이 없거나, plan task가 식별 안 됨.
  - 조치: spec/plan에 유스케이스 단위 섹션 또는 `## Acceptance Criteria` 섹션을 추가한 후 재호출하거나, 다른 파일을 사용하세요.
  ```

### 파일 없음 / 빈 파일

Step 1에서 markdown_path가 존재하지 않거나 빈 파일이면:
```
❌ 입력 파일을 읽을 수 없습니다: <markdown_path>
```
즉시 종료.

### `~/Desktop/` 쓰기 권한 없음

Step 9에서 Write 실패 시:
```
❌ ~/Desktop/ 쓰기 권한 없음. macOS Settings → Privacy → Full Disk Access 확인 필요.
```

### HTML 템플릿 없음

Step 3에서 template.html 없으면:
```
❌ HTML 템플릿 없음: ~/.claude/skills/html-review/assets/template.html
   → /html-review skill이 정상 설치되었는지 확인하세요.
```
````

- [ ] **Step 3: 파일 생성 검증**

Run:
```bash
ls -la ~/dotfiles/.claude/agents/spec-to-feature-review-builder.md
wc -l ~/dotfiles/.claude/agents/spec-to-feature-review-builder.md
```
Expected: 파일 존재 + 약 180~220줄.

- [ ] **Step 4: 마크다운 frontmatter 유효성 빠른 검증**

Run:
```bash
head -8 ~/dotfiles/.claude/agents/spec-to-feature-review-builder.md
```
Expected: `---`로 시작, `name: spec-to-feature-review-builder`, `description:`, `model: sonnet`, `---` 종료 포함.

- [ ] **Step 5: Commit**

```bash
cd ~/dotfiles
git add .claude/agents/spec-to-feature-review-builder.md
git commit -F /tmp/commit-task1.txt
```

먼저 `/tmp/commit-task1.txt`에 메시지 작성 후 commit:
```
feat(claude): add spec-to-feature-review-builder sub-agent

- spec/plan markdown → use case 추출 → Gherkin Feature 변환 → HTML 리뷰 페이지 생성
- 영어 Gherkin 키워드 + 한국어 본문 규칙 적용
- 변형 A delegation 패턴 (sonnet sub-agent)
- html-review template 재사용으로 인터랙티브 리뷰 UI 통일
```

---

## Task 2: Slash command (`spec-to-feature-review.md`) 작성

**Files:**
- Create: `~/dotfiles/.claude/commands/spec-to-feature-review.md`

Slash command는 변형 A delegation으로 sub-agent에 위임만 한다. 로직 없음.

- [ ] **Step 1: 디렉토리 존재 확인**

Run:
```bash
ls -d ~/dotfiles/.claude/commands/ 2>&1
```
Expected: 디렉토리 listing (이미 존재).

- [ ] **Step 2: 파일 생성**

Write 도구로 `~/dotfiles/.claude/commands/spec-to-feature-review.md`에 저장:

````markdown
---
description: spec/plan markdown 문서에서 use case별 Gherkin Feature 파일을 추출하고 인터랙티브 HTML 리뷰 페이지를 생성. 영어 Gherkin 키워드 + 한국어 본문.
argument-hint: <markdown-file-path>
allowed-tools: [Read, Write, Edit, Bash, Agent]
---

# spec-to-feature-review

spec/plan markdown(verbose) → use case 추출 → primary + alternative scenarios → Gherkin Feature 변환 → /html-review 스타일 인터랙티브 리뷰 페이지.

## 실행 모델 (필수)

**~/.claude/templates/delegation.md 변형 A 적용** — 단, `subagent_type`은 `general-purpose` 대신 **`spec-to-feature-review-builder`** 사용 (전용 sub-agent).
(model="sonnet", run_in_background=false, args=`$ARGUMENTS`)

main context에서 직접 실행 금지.

## 사용법

| 호출 방식 | 설명 |
|---|---|
| `/spec-to-feature-review <markdown-path>` | 지정 spec/plan 파일에서 Gherkin 추출 + HTML 리뷰 페이지 생성 |

예시:
```
/spec-to-feature-review docs/superpowers/specs/2026-05-17-foo-design.md
/spec-to-feature-review docs/superpowers/plans/2026-05-17-foo.md
```

## 위임 절차

Main context가 할 일:

1. `$ARGUMENTS`에서 첫 positional 인자를 `markdown_path`로 추출.
2. 인자가 없으면 사용자에게 파일 경로 요청 후 종료.
3. `spec-to-feature-review-builder` agent에 위임:
   - `subagent_type`: `spec-to-feature-review-builder` (전용 agent)
   - `model`: `sonnet`
   - `run_in_background`: false
   - prompt: `markdown_path: <추출한 경로>`
4. agent 결과를 그대로 사용자에게 보고.

## 위임 시 전달할 정보

```
markdown_path: <첫 positional 인자>
```

## 산출물

- `<spec-dir>/features/<basename>.feature` — Gherkin Feature 파일
- `~/Desktop/feature-review-<basename>-<YYYYMMDD>.html` — 인터랙티브 리뷰 페이지

## Failure Conditions

- 인자 미제공 → 사용자에게 파일 경로 요청
- 그 외 모든 실패는 sub-agent의 Failure Conditions 그대로 보고
````

- [ ] **Step 3: 파일 생성 검증**

Run:
```bash
ls -la ~/dotfiles/.claude/commands/spec-to-feature-review.md
head -6 ~/dotfiles/.claude/commands/spec-to-feature-review.md
```
Expected: 파일 존재 + frontmatter (`description:`, `argument-hint:`, `allowed-tools:`) 포함.

- [ ] **Step 4: Commit**

`/tmp/commit-task2.txt`에 메시지 작성:
```
feat(claude): add /spec-to-feature-review slash command

- spec-to-feature-review-builder sub-agent를 위임 호출하는 thin wrapper
- 변형 A delegation 패턴 (model="sonnet")
- argument-hint: <markdown-file-path>
```

Run:
```bash
cd ~/dotfiles
git add .claude/commands/spec-to-feature-review.md
git commit -F /tmp/commit-task2.txt
rm /tmp/commit-task2.txt
```

---

## Task 3: Dogfooding 검증

**Files:**
- Read: `docs/superpowers/specs/2026-05-17-spec-to-feature-review-tool-design.md`
- Generated: `docs/superpowers/specs/features/2026-05-17-spec-to-feature-review-tool-design.feature`
- Generated: `~/Desktop/feature-review-2026-05-17-spec-to-feature-review-tool-design-<YYYYMMDD>.html`

이 도구로 자기 자신의 spec을 변환하여 spec의 4개 acceptance scenario를 검증한다.

- [ ] **Step 1: 도구 호출**

새 Claude Code 세션에서 (또는 같은 세션에서 직접):
```
/spec-to-feature-review docs/superpowers/specs/2026-05-17-spec-to-feature-review-tool-design.md
```

Expected: 도구가 sub-agent를 invoke하고 약 30~60초 후 완료 보고 + 브라우저 자동 열림.

- [ ] **Step 2: Acceptance Scenario 1 검증 — `.feature` 파일 생성**

Spec acceptance scenario:
```
Scenario: 도구가 유스케이스를 추출하여 Gherkin과 HTML 리뷰를 생성한다
  Given spec 또는 plan markdown 파일이 <file> 경로에 존재한다
  When 사용자가 /spec-to-feature-review <file>을 호출한다
  Then .feature 파일이 <spec-dir>/features/<basename>.feature에 생성된다
  And .feature는 식별된 유스케이스마다 하나의 Feature를 포함한다
  And 각 Feature는 1개의 primary scenario와 ≥1개의 alternative scenario를 포함한다
```

Run:
```bash
ls -la docs/superpowers/specs/features/2026-05-17-spec-to-feature-review-tool-design.feature
cat docs/superpowers/specs/features/2026-05-17-spec-to-feature-review-tool-design.feature | head -50
```

Expected:
- 파일 존재
- `Feature:` 키워드가 ≥1개 (use case별)
- 각 Feature 아래 `Scenario:` 키워드가 ≥2개 (primary + alternative)
- 키워드는 영어, 본문은 한국어

- [ ] **Step 3: Acceptance Scenario 1 검증 — HTML 페이지**

```
Then HTML 리뷰 페이지가 브라우저에서 열린다
And HTML 페이지는 /html-review UX를 미러링한다 (항목별 👍/🤔/🔁/❌ + 코멘트 + export)
```

Run:
```bash
TODAY=$(date +%Y%m%d)
ls -la ~/Desktop/feature-review-2026-05-17-spec-to-feature-review-tool-design-${TODAY}.html
```

Expected: 파일 존재 (`open` 명령으로 이미 브라우저 열림).

브라우저에서 수동 확인:
- [ ] 페이지 상단에 "Feature Review: 2026-05-17-spec-to-feature-review-tool-design" 타이틀
- [ ] 좌측 sidebar에 Feature 목록 (TOC)
- [ ] 각 Scenario 항목에 👍/🤔/🔁/❌ 버튼
- [ ] 코멘트 입력란
- [ ] 하단 export bar에 "Copy as next prompt" 버튼

- [ ] **Step 4: Acceptance Scenario 3 검증 — next-prompt export**

```
Scenario: 사용자가 피드백을 next-prompt로 export한다
  Given HTML 리뷰 페이지가 추출된 시나리오들과 함께 열려 있다
  When 사용자가 시나리오에 👍/🤔/🔁/❌를 표시하고 "Copy as next prompt"를 클릭한다
  Then 클립보드에 시나리오별 피드백이 포함된 구조화된 프롬프트가 복사된다
```

브라우저에서 수동 검증:
- [ ] 임의 시나리오 1개에 👍 클릭
- [ ] 다른 시나리오 1개에 🔁 클릭 + 코멘트 "테스트 코멘트" 입력
- [ ] "Copy as next prompt" 버튼 클릭
- [ ] 클립보드를 다른 곳(텍스트 에디터)에 붙여넣어 내용 확인:
  - 시나리오 ID와 status (👍/🔁)
  - 코멘트 내용

기록: 어느 시나리오에 어떤 status를 줬는지 + 클립보드 내용 일부를 plan markdown 또는 journal에 메모.

- [ ] **Step 5: Acceptance Scenario 4 검증 — 덮어쓰기**

```
Scenario: 기존 .feature 파일은 안전하게 덮어쓴다
  Given 대상 경로에 .feature 파일이 이미 존재한다
  When 사용자가 같은 spec으로 /spec-to-feature-review를 다시 호출한다
  Then 새 .feature가 기존 파일을 대체한다
  And 응답에 한 줄로 "기존 파일 덮어씀" 안내가 포함된다
```

Run (동일 명령 재호출):
```
/spec-to-feature-review docs/superpowers/specs/2026-05-17-spec-to-feature-review-tool-design.md
```

Expected: 결과 보고에 `ℹ️  기존 .feature 파일을 덮어썼습니다.` 한 줄 포함.

`.feature` 파일 mtime 확인:
```bash
stat -f '%m %N' docs/superpowers/specs/features/2026-05-17-spec-to-feature-review-tool-design.feature
```
Expected: mtime이 방금(재호출 시점)으로 갱신됨.

- [ ] **Step 6: Acceptance Scenario 2 검증 — no use cases found**

```
Scenario: 추출 가능한 유스케이스가 없을 때
  Given markdown 파일에 식별 가능한 유스케이스가 없다
  When 사용자가 /spec-to-feature-review <file>을 호출한다
  Then .feature 파일이 생성되지 않는다
  And 응답에 "유스케이스를 추출할 수 없음" 사유가 포함된다
```

테스트용 빈 markdown 작성:
```bash
cat > /tmp/empty-spec.md <<'MD'
# Empty Spec

This document has no use cases. Just prose.

## Background

Some background paragraph that does not describe any actionable behavior.
MD
```

호출:
```
/spec-to-feature-review /tmp/empty-spec.md
```

Expected:
- `.feature` 파일 **생성 안 됨**:
  ```bash
  ls /tmp/features/empty-spec.feature 2>&1
  # → No such file or directory
  ```
- 응답 메시지에 `⚠️ 유스케이스를 추출할 수 없습니다.` 및 사유 포함

테스트 후 cleanup:
```bash
rm /tmp/empty-spec.md
```

- [ ] **Step 7: Dogfooding 결과 기록**

`~/.claude/journals/2026-05.journal.md`에 dogfooding 결과 append:

```
## 2026-05-17 HH:MM | dotfiles | spec-to-feature-review tool dogfooding

도구를 자기 자신의 spec(`2026-05-17-spec-to-feature-review-tool-design.md`)으로 변환.
- Acceptance scenario 1 (Gherkin + HTML): {pass|fail}
- Acceptance scenario 2 (no use cases): {pass|fail}
- Acceptance scenario 3 (next-prompt export): {pass|fail}
- Acceptance scenario 4 (overwrite): {pass|fail}
- 추출된 Feature 수: {N}개, Scenario 수: {M}개
- 사용자 체감: Gherkin 리뷰가 spec markdown 리뷰보다 {효율적|애매|비효율}
```

- [ ] **Step 8: 생성된 .feature 커밋 (선택)**

생성된 .feature를 spec과 함께 보존하려면 commit:

`/tmp/commit-task3.txt`:
```
docs(spec): add Gherkin feature file for spec-to-feature-review tool

- /spec-to-feature-review로 자기 자신의 spec을 변환하여 생성
- dogfooding 결과: 4개 acceptance scenario 모두 검증 완료
- 사용자 리뷰 인터페이스 = Gherkin (spec markdown 본문 보지 않고 행위만)
```

```bash
cd ~/dotfiles
git add docs/superpowers/specs/features/2026-05-17-spec-to-feature-review-tool-design.feature
git commit -F /tmp/commit-task3.txt
rm /tmp/commit-task3.txt
```

생성된 .feature를 보존하지 않으려면 (디스크상 유지하되 git 추적 제외) `.gitignore`에 `docs/superpowers/specs/features/` 추가 후 commit. 본 plan default는 **commit** (참고 자료로 보존).

---

## Self-Review

**1. Spec coverage:**

| Spec 섹션/요구 | 구현 task |
|---|---|
| 1. 문제 정의 | Task 3 dogfooding으로 핵심 가치 검증 |
| 2. 파이프라인 5단계 | Task 1 Step 2의 sub-agent system prompt Step 1~10 |
| 3.1 Slash command + sub-agent | Task 1 (sub-agent), Task 2 (command) |
| 3.2 입출력 경로 | Task 1 sub-agent prompt Step 6, Step 9 |
| 3.3 영어 키워드 + 한국어 본문 | Task 1 sub-agent prompt "핵심 규칙" 섹션 |
| 4. 동작 흐름 7단계 | Task 1 sub-agent prompt Step 1~10 |
| 5. 기존 자산 활용 | Task 1 sub-agent prompt Step 3, Step 8 |
| 6. Acceptance Criteria 4개 | Task 3 Step 2~6 검증 |
| 7. 변경 인벤토리 2개 | Task 1, Task 2 |
| 8. Out of Scope | plan에서 의도적으로 미포함 (CLAUDE.md wiring 등) |
| 9. 의존성 | Task 1 sub-agent prompt Failure Conditions |

모든 spec 요구사항이 task에 매핑됨. 갭 없음.

**2. Placeholder scan:**

- "TBD/TODO/implement later" — 없음
- "add appropriate X" — 없음
- "similar to Task N" — 없음 (각 task 자기 완결)
- 코드 step 모두 실제 내용 포함 (sub-agent prompt 전체, slash command 전체)

**3. Type consistency:**

- `markdown_path` (sub-agent 입력) — Task 1 Step 2와 Task 2 Step 2 모두 동일 변수명 사용
- `today_yyyymmdd` — Task 1 Step 2(획득)와 Step 9(사용) 일치
- `.feature` 출력 경로 형식 `<spec-dir>/features/<basename>.feature` — Task 1 Step 6 정의, Task 3 Step 2~5 모두 동일 사용
- HTML 출력 경로 `~/Desktop/feature-review-<basename>-<YYYYMMDD>.html` — Task 1 Step 9 정의, Task 3 Step 3 동일 사용
- REVIEW_DATA 구조 — Task 1 Step 7(생성)과 template.html(소비) placeholder `{{REVIEW_DATA}}` 일치
- sub-agent name `spec-to-feature-review-builder` — Task 1 frontmatter, Task 2 위임 절차, Task 3 도구 호출 모두 동일

일관성 검증 완료. 발견된 문제 없음.

---

## Notes

- 이 plan은 minimal MVP. CLAUDE.md wiring, trial protocol, mutation testing 등은 spec Section 8 "Out of Scope"에 따라 의도적으로 제외.
- Task 1의 sub-agent prompt는 plan 안에서 완전한 형태로 제공됨 — 별도 추론 불필요.
- Task 3 dogfooding이 핵심. 4개 acceptance scenario 모두 통과해야 도구가 spec 요구사항을 충족했다고 판단.
- 도구 효익 체감 후 향후 확장 결정: 자동 호출, CLAUDE.md wiring, 다른 spec/plan 변환 사례 축적.
